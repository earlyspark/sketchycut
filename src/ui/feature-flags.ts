/**
 * The exact measurement and packed-span calibration flows remain implemented,
 * but the public workbench intentionally starts and stays on the registered
 * starter setup until product direction re-enables this chooser.
 */
export const PUBLIC_GUIDED_FIT_MODES_ENABLED = false;
